/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', '192.168.100.54'], // domains de onde quero que carregue imagens
  },
}

module.exports = nextConfig
